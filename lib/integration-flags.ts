/** Keys mirrored from `GET /api/platform/meta` → `integrations` */
export type IntegrationFlags = {
  coingecko?: boolean;
  finnhubQuote?: boolean;
  finnhubNews?: boolean;
  yahooKospi?: boolean;
};
